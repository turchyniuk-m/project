The full dataset is stored in Google BigQuery due to size and privacy restrictions.  

To reproduce the analysis, use the SQL query provided in `sql/main_query.sql`.  
All data transformations, aggregations, and visualizations are implemented in the notebooks.
